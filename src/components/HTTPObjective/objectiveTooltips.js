const tooltips = {
  applicationLayer: `An application layer is an abstraction layer (a way of hiding the working
  details of a subsystem) that specifies the shared communication protocols and interface methods
  used by hosts in a communications network.`,
  protocol: `A protocol is a system of rules that define how data is exchanged within or between
  computers. Communications between devices require that the devices agree on the format of the data
  that is being exchanged. The set of rules that defines a format is called a protocol.`,
};

export default tooltips;
